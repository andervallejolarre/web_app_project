import { t as __commonJSMin } from "./chunk-CYJPkc-J.js";
import { t as require_react } from "./react.js";
//#region node_modules/react-geolocated/dist-modules/index.js
var require_dist_modules = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.useGeolocated = useGeolocated;
	var react_1 = require_react();
	/**
	* Hook abstracting away the interaction with the Geolocation API.
	* @param config - the configuration to use
	*/
	function useGeolocated(config = {}) {
		const { positionOptions = {
			enableHighAccuracy: true,
			maximumAge: 0,
			timeout: Infinity
		}, isOptimisticGeolocationEnabled = true, userDecisionTimeout = void 0, suppressLocationOnMount = false, watchPosition = false, geolocationProvider = typeof navigator !== "undefined" ? navigator.geolocation : void 0, watchLocationPermissionChange = false, onError, onSuccess } = config;
		const userDecisionTimeoutId = (0, react_1.useRef)(0);
		const isCurrentlyMounted = (0, react_1.useRef)(true);
		const watchId = (0, react_1.useRef)(0);
		const [isGeolocationEnabled, setIsGeolocationEnabled] = (0, react_1.useState)(isOptimisticGeolocationEnabled);
		const [coords, setCoords] = (0, react_1.useState)();
		const [timestamp, setTimestamp] = (0, react_1.useState)();
		const [positionError, setPositionError] = (0, react_1.useState)();
		const [permissionState, setPermissionState] = (0, react_1.useState)();
		const sameCoords = (0, react_1.useCallback)((a, b) => {
			if (a === b) return true;
			if (!a || !b) return false;
			return a.accuracy === b.accuracy && a.altitude === b.altitude && a.altitudeAccuracy === b.altitudeAccuracy && a.heading === b.heading && a.latitude === b.latitude && a.longitude === b.longitude && a.speed === b.speed;
		}, []);
		const updateCoords = (0, react_1.useCallback)((next) => {
			setCoords((prev) => {
				if (sameCoords(prev, next)) return prev;
				return next;
			});
		}, [sameCoords]);
		const cancelUserDecisionTimeout = (0, react_1.useCallback)(() => {
			if (userDecisionTimeoutId.current) window.clearTimeout(userDecisionTimeoutId.current);
		}, []);
		const handlePositionError = (0, react_1.useCallback)((error) => {
			cancelUserDecisionTimeout();
			if (isCurrentlyMounted.current) {
				setCoords(() => void 0);
				setIsGeolocationEnabled(false);
				setPositionError(error);
			}
			onError === null || onError === void 0 || onError(error);
		}, [onError, cancelUserDecisionTimeout]);
		const handlePositionSuccess = (0, react_1.useCallback)((position) => {
			cancelUserDecisionTimeout();
			if (isCurrentlyMounted.current) {
				updateCoords(position.coords);
				setTimestamp(position.timestamp);
				setIsGeolocationEnabled(true);
				setPositionError(() => void 0);
			}
			onSuccess === null || onSuccess === void 0 || onSuccess(position);
		}, [
			onSuccess,
			cancelUserDecisionTimeout,
			updateCoords
		]);
		const getPosition = (0, react_1.useCallback)(() => {
			if (!(geolocationProvider === null || geolocationProvider === void 0 ? void 0 : geolocationProvider.getCurrentPosition) || !geolocationProvider.watchPosition) throw new Error("The provided geolocation provider is invalid");
			if (userDecisionTimeout && permissionState !== "granted") userDecisionTimeoutId.current = window.setTimeout(() => {
				handlePositionError();
			}, userDecisionTimeout);
			if (watchPosition) watchId.current = geolocationProvider.watchPosition(handlePositionSuccess, handlePositionError, positionOptions);
			else geolocationProvider.getCurrentPosition(handlePositionSuccess, handlePositionError, positionOptions);
		}, [
			geolocationProvider,
			watchPosition,
			userDecisionTimeout,
			handlePositionError,
			handlePositionSuccess,
			positionOptions,
			permissionState
		]);
		(0, react_1.useEffect)(() => {
			let permission = void 0;
			if (watchLocationPermissionChange && geolocationProvider && "permissions" in navigator) navigator.permissions.query({ name: "geolocation" }).then((result) => {
				permission = result;
				permission.onchange = () => {
					if (permission) setPermissionState(permission.state);
				};
			}).catch((e) => {
				console.error("Error updating the permissions", e);
			});
			return () => {
				if (permission) permission.onchange = null;
			};
		}, []);
		(0, react_1.useEffect)(() => {
			if (!suppressLocationOnMount) getPosition();
			return () => {
				cancelUserDecisionTimeout();
				if (watchPosition && watchId.current) geolocationProvider === null || geolocationProvider === void 0 || geolocationProvider.clearWatch(watchId.current);
			};
		}, [permissionState]);
		return {
			getPosition,
			coords,
			timestamp,
			isGeolocationEnabled,
			isGeolocationAvailable: Boolean(geolocationProvider),
			positionError
		};
	}
}));
//#endregion
export default require_dist_modules();

//# sourceMappingURL=react-geolocated.js.map